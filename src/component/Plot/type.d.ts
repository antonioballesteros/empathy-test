export interface Point {
  x: number;
  y: number;
}

export interface PlotValue {
  id?: number;
  x: number;
  y: number;
  label?: string;
}

export interface Literal {
  color: string;
  label: string;
}
